schema {
  query: Query
  mutation: Mutation
}

type Exercise {
  id: ID!
  desciption: String!
}

type Query {
  exercises: String!
  exercise(exerciseId: ID!): Exercise!
}

type Mutation {
  addExercise(desciption: String!): Exercise
}
