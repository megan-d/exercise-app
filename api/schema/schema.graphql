schema {
  query: Query
  mutation: Mutation
}

type Exercise {
  id: String!
  description: String!
}

type Query {
  getExercises: String!
  getExercise(exerciseId: ID!): Exercise!
}

type Mutation {
  addExercise: String
}
